import EmailBody from "./EmailBody";

const EmailDraft = () => {
  return (
    <section>
      <h2 className="text-xl mb-2">Create a Draft</h2>
      <section>
        <EmailBody />
      </section>
    </section>
  );
};

export default EmailDraft;
